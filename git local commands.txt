install git and use locally commands:
git config --global user.name "Your Name"
git config --global user.email "you@example.com"
git config --global --list
git init 
git log
git status
git diff
git add file name or * for all
git commit  with git commit -m "name of commitment"
git show commit id:file path
git switch -c
git checkout commit id -- *

git checkout commit id : filepath
git checkout master -- * or filepath // master is the branch where you want to rollback changes.
git restore . // . is for all files restoration
git restore filepath // for specific file.

git reset --soft Head^
git reset --hard Head^ // to reset and delete changes as well

 git log -p -1 // for last 1 commit with difference.number can be increase for last increase commits

git log --stat // for summary of changes.

git log --pretty=oneline // to view merged and easy view.
git log --pretty=format"%h-%an, %ar:%s"

git log -S function name like h1 for h1 tag
